{
  "name": "cycle-deepstream",
  "version": "1.0.0",
  "description": "A Cycle.js driver for deepstream.io",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "dist": "rimraf dist && tsc",
    "postinstall": "npm run dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "simple": "cd examples/simple && npm install && npm run start",
    "comprehensive": "cd examples/comprehensive && npm install && npm run start"
  },
  "keywords": [
    "cyclejs",
    "deepstream"
  ],
  "author": "Ryan McGuire <ryan@enigmacurry.com>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.2.2"
  },
  "dependencies": {
    "@cycle/run": "^3.0.0",
    "deepstream.io-client-js": "^2.1.2",
    "events": "^1.1.1",
    "xstream": "^10.0.0"
  }
}
